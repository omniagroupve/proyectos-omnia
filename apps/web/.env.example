# En desarrollo se deja vacío: Vite hace proxy de /api al backend en :3000.
# En producción apunta al backend desplegado (https://api.pix.com).
VITE_API_URL=

# Supabase — las MISMAS claves públicas que usa el backend.
# Nunca pongas aquí SUPABASE_SERVICE_ROLE_KEY: este bundle va al navegador.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
